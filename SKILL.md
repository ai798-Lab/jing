---
name: jing
description: 镜：四面镜子（以乔布斯为镜照产品力 / 以王小波为镜照自欺 / 以库布里克为镜照完成度 / 以第一性原理为镜照事实）定期审视一段时间的工作与决策，输出镜鉴纪要并为该月上一字谥号。触发词：照镜、照一下这个月、照一下这周、镜、复盘我的一周/一月/季度、我在浪费生命吗。数据源：Claude Code 会话（~/.claude/projects/）、Codex 会话（~/.codex/，如有）、memory 工作线、产出物文件与 git 历史。
---

# 镜

以铜为镜，可以正衣冠；以史为镜，可以知兴替；以人为镜，可以明得失。一人公司没有魏征，这四面镜子就是给自己立的。

## 归档约定

先定两个位置，全文用到：

- **归档目录**：镜鉴 HTML 与基线库的落盘处，建议固定一处（如 `~/jing-archive/`）。首次照镜时与照镜人确认，之后不变。
- **基线库**：`<归档目录>/jing-baseline.json`。每期照镜的计数、词频、问题清单、决议、预测都写进去，是跨月对比的唯一数据源。

## 原则（硬规则）

1. **只照见，不安慰。**每条判断必须引用具体证据：某个会话、某个文件、某段时间数据。说不出证据的意见不进纪要。
2. **四镜先各照各的，再合。**每面镜子独立成像之前不允许看彼此的输出（防趋同）。镜与镜相互矛盾是特性不是 bug，纪要必须保留分歧。
3. **输出是镜鉴不是报告。**有共识、有分歧、有对照镜人问题的直接回答、有决议和下次跟踪项，末尾上谥。
4. **成本纪律。**日常小照用单镜快照（一个 agent 依次过四面镜子）；只有月度、季度或重大决策才开全镜（四镜独立 agent）。
5. **本人测试（实照教训）。**每条观察交稿前问一遍：照镜人自己五分钟能总结出这条吗？能，就删。镜只留「有证据的意外」。考勤表级的结论（哪条线停了、哪个数字没看）本人全知道，不配进纪要。
6. **照人，不只照事。**项目清单是事，镜子照的是人：下指令的方式、打回产出的理由、高频的批评词、重复的行为怪癖、说法随时间的漂移。行为层数据（会话内的 user 消息流）比元数据（会话数量/文件清单）值钱一个量级。
7. **指标的构造效度（第三照的教训，代价最高的一条）。**任何自造指标在用它下结论之前，必须先回答：**这个指标的定义，会不会让结论变成同义反复？**真实翻车案例：某次照镜造了「AI 自驱返工率」，定义是「两次写入之间没夹真人消息的比例」，于是「人离场时该比例趋近 100%」成了算术必然，却被四镜包装成「人一离场 AI 就加速」的反直觉发现，还配了图。复核回原始数据发现，这些所谓的「返工轮次」里 74% 在往文件里净增内容，根本不是返工。**自查三问：①这个名字承诺度量的东西，和它实际度量的东西是同一件吗 ②抽 3 个极端样本回原始数据手验一遍 ③换一个中性名字重述结论，它还成立吗。**过不了就改名，改名后站不住就删。
8. **越抓眼球的发现，验得越狠。**「与常识相反」是危险信号不是加分项。凡结论带「反直觉」「与常识相反」「其实恰恰相反」的，必须额外做一次原始数据亲验，并在纪要里公示亲验记录。

## 数据收集（月度照镜标准流程）

0. **回看旧账（从第二期起必须，先于一切新取证）**：读过去两期镜鉴 `jing-YYYY-MM.html` 与基线库，提取三张底稿：①上期暴露的核心问题清单 ②上期决议与钩子 ③上期可证伪预测。本期镜鉴必须逐条裁决，不许只报新观察不回旧账。
1. **时间与分布**（只统计元数据，不读全文）：`~/.claude/projects/*/` 时间窗内 jsonl 的数量、大小、mtime 分布；如用 Codex，另加 `~/.codex/session_index.jsonl` 与 `~/.codex/sessions/YYYY/MM/`。产出：项目 × 时间的工作分布表。注意两个坑：按每条消息自身 timestamp 落窗，不用文件 mtime（会被批量进程触碰）；区分真人消息与自动化消息（SDK / 工作流 / 任务通知），只有真人消息算行为指纹。
2. **语言指纹与决策流（核心，最值钱的一层）**：选最大与最关键的 10 至 15 个会话，python 流式提取全部 user 消息（只要 user，assistant 输出不要），分析：高频反馈词与批评词、打回产出的时刻及理由原话、指令风格、A/B 抉择时的选择与犹豫、说法随会话推进的变化。这是照镜人的行为指纹，纪要里最深的观察都从这层来。
3. **产出物实检（不看清单，打开看）**：抽 8 至 12 个代表性产出文件真打开逐帧检查：排版一致性、错字、死链、收尾质量、命名。给每件出「亮点 / 偷懒处」两栏实检记录。
4. **言行差账本**：读全部 memory 工作线文件，提取所有「计划 / 待办 / 下一步」表述，逐条核对实际发生了什么（文件与 git 证据），输出三类：说了没做、做了没说、说法漂移。照镜人没开 memory 时，此层跳过并在镜鉴里声明，不硬凑。
5. **产出物盘点与纵向对比**：新文件清单、git log；若有上期数据，做环比（变化才是信息）。
6. **双侧对照：AI 产出的命运（user 侧只有一半真相）**：前五层都只统计「产出了什么」，这层统计「产出之后发生了什么」。追踪代表性产出的命运四判定：被采纳沿用 / 被打回重做 N 版 / 被扔掉 / 完工后从未被打开。重点抓三类证据：①**打回案例的 v1 与终版 diff**（骂声只说明照镜人不要什么，diff 才说明他要什么，这是品味的实体，也是整个 skill 里最值钱的一层）②同一文件被反复重写的热点，区分打磨与空转（判据：重写前有没有真人消息，以及净增字符是正是负）③AI 反复犯、照镜人反复骂同一句的循环，产出「教法失效清单」。
7. **协作病理与浪费账本（照人机界面本身，不照工作内容）**：照镜人怎么用 AI 的结构性习惯：spec 完整度与返工率的关系、需求变更的时机分布（是不是全发生在看到成品之后）、判据可传递性（打回理由能否写成规则，还是只能长在眼睛里）。同时清算 AI 侧浪费：跑了没人读的产出、被中断丢弃的长任务、无人要求的主动劳动、跨会话重复劳动，逐条估算代价。**每条浪费必须转化为一条可执行禁令或流程改进**（「以后不让 AI 做 X」「做 X 前必须 Y」），作为规则文件的候选修正案交照镜人拍板。禁令必须带触发阈值，避免误伤健康的短会话。
8. **工具存量盘点：skill / MCP / 插件的使用率与加载成本**：照镜人装了多少工具，其中多少是活的？
   **先理解成本结构，三类完全不同，混算必错**：
   - **skill**：常驻成本只是清单里一行描述，调用时才注入全文。
   - **MCP**：**工具的完整 JSON Schema 常驻在系统提示里，不调用也占位**。一个 server 二三十个工具，成本比 skill 高一个数量级。所以零调用的 MCP 比零调用的 skill 贵得多。注意区分常驻与按需加载（deferred）两种模式，成本模型不同。
   - **插件**：可能同时带 skill、MCP、命令与 hook，必须拆开算。尤其查**通过 SessionStart 一类 hook 往每个会话注入全文**的插件，它的成本是「注入字符 × 全部会话数」，且完全无声。
   
   统计每项近三个月的真实调用次数与最后一次调用时间，分四档：**高频真用 / 低频 / 长期零调用 / 自建但从未被自己用过**（最后一档最刺眼，单独列表并标注建成至今天数）。另查三类反常：①**无效加载**（工具被载入上下文却没真正走它的流程）②**该调没调**（有对应工具却手工重做了一遍）③**鼓励过度调用的指令**（某些工具包会写「有一点相关就必须调用」这类指令，它本身就是无效加载的制造者，要单独验证它的净收益）。结论给三张清单：该退休的 / 该改触发词的 / 该合并的，每条标明删掉能省多少、风险是什么。
   **诚实边界**：工具的价值不等于使用频率，为长尾场景准备的工具零调用是正常的。判据是「它服务的那件事发生时，它被想起了吗」，不是「它跑了几次」。凡建议删除的，必须说明在这个判据下为什么可以删。
9. **踩坑萃取率：踩了之后是长记性还是反复踩**：建坑清单（20 至 30 个有代表性的失败与返工，分技术坑 / 协作坑 / 判断坑），逐个核查三件事：①**萃取判定**（这个坑有没有变成可复用的东西？去 CLAUDE.md、SKILL.md、memory、代码注释里 grep，三档：已萃取 / 口头说过没落盘 / 完全没萃取）②**复发判定**（之后有没有再踩）③**交叉出四象限**：已萃取且未复发（有效）/ **已萃取但仍复发（萃取无效，最值钱的一格）** / 未萃取且复发（纯浪费）/ 未萃取但没复发（偶发，不必防）。对「已萃取但仍复发」的逐个分析为什么没挡住：写在了错误的位置？写成了不可执行的原则？没有触发点？最后给 5 至 8 个可避免坑的**具体防复发方法**（写进哪个文件的哪一行 / 加什么检查脚本 / 什么时机自动触发），禁止「注意一下」「加强检查」这类废话；同时诚实标出「不值得防的坑」（防它的成本高于踩它）。
10. **判断力的行为指纹（照人自己这一侧，本 skill 最敏感的一层）**：前九层照的都是工作与 AI，这一层照照镜人自己：他的判断力是在变强还是在被 AI 稀释？
   **方法论警告，不遵守这一层必产假指标**：naive 做法是统计「按你的来 / 继续 / 好」这类消息的占比，这是错的，它把**表达成本**当成了**判断含量**。「A」是一次拍板，可能是全月最重的决策；长消息也可能零判断。更关键的是**委托不等于退化**，把不该自己做的事交出去是正确分工。
   **正确的核心判据**：
   > 「按你的来」本身没问题。「按你的来」之后再也没打开过那个产物，才是问题。
   
   健康的委托与真正的放弃，在消息文本上一模一样，区别只在于**之后有没有回来看**。这把一个不可测的心理状态（有没有在思考）换成了一个可测的行为（有没有回访）。所以核心指标是**放行后的回访率**，不是放行占比。
   
   配三条逐月趋势信号看衰减：①**打回时自带正样本的比例**（打回同时给出具体判据、替代结构或参照物，这是判断力强的直接证据，看它是否在稀释）②**回应里引用产物具体细节的比例**（页码、行号、原文，区分「真看了」与「扫了一眼」）③**追问密度**（「为什么」类问题的千分比）。
   再加两条反向证据：**照镜人自己查证、自己动手、纠正 AI 事实错误的次数**（上升则退化假说被证伪）；以及**交叉判定**：反驳 AI 的比例下降可能是 AI 变准了而非人变懒了，必须与「事后被证明 AI 错了的次数」交叉，两者同时恶化才算退化信号。
   **结论纪律**：数据不支持退化就明说不支持，并给出它反而在加强的证据。这一层的价值不在于确认担忧，在于给出一个能逐月跟踪的真指标。
11. 汇成数据包：分布表、语言指纹、实检记录、言行差账本、双侧对照、浪费账本、工具存量、坑账本、判断力指纹、决策清单、烂尾清单。
12. **数据不足熔断**：时间窗内真人消息不足约 200 条时，明确告知照镜人数据不足、只出数据速览不出四镜成像。镜宁可不照，不许硬凑。

## 四镜（每面：判断规则 + 典型照法 + 失效条件）

### 01 以乔布斯为镜 · 照产品力

- **照什么：你做的东西该不该存在？**
- 判断规则：严格区分产品（有真实用户、有验收标准、在生产环境跑）与玩具（自嗨、演示完就死）；砍到不能再砍；一个周期只能有一条主线。
- 典型照法：这个月做的这些事里，哪件是产品哪件是玩具？哪个妥协你说不出口？如果只许留一件事，留哪件、凭什么？你做的工具的工具的工具，最终的用户到底在哪？
- 失效条件：对纯学习期、研究期的探索会误判为「该砍」；对回报周期长的基建会系统性低估。照探索性工作时降低音量。

### 02 以王小波为镜 · 照自欺

- **照什么：你有没有在骗自己？**
- 判断规则：识别表演性忙碌（产出很多但从不验收）；识别从众（因为圈子都在做所以做）；看沉默的证据（回避的事、不敢看的数字、从不提的项目）。
- 典型照法：哪些工作本质是做给别人看的？哪个判断是从时间线上抄来的？你一直回避验收的项目是哪个？把「AI」这个词从你的月度里拿掉，还剩下什么？
- 失效条件：过度怀疑会否定一切积累期投入；这面镜子必须带幽默感，否则会变刻薄而失去照镜人。

### 03 以库布里克为镜 · 照完成度

- **照什么：细节配得上野心吗？**
- 判断规则：看完成度不看数量；逐帧看（抽查具体产出的具体细节，不接受整体印象）；重复出现的偷懒模式比单次瑕疵严重一个量级。
- 典型照法：这个月最好的一件产出是什么？它经得起逐帧暂停吗？哪一类细节你在系统性偷懒（排版？命名？收尾？）？你的产出有署名感吗：拿掉名字，别人认得出是你做的吗？
- 失效条件：对快速原型和一次性探索要求完美是错的；完成度不能凌驾于 shipped，完美主义拖延要被这面镜子自己照出来。

### 04 以第一性原理为镜 · 照事实

- **照什么：剥掉类比和惯性，事实是什么？**
- 判断规则：只认数据（时间分布、产出计数、完成率），不认叙述；目标链条回溯（每件事向上追问三层「为什么做」）；从零重建测试（今天从零开始，你还会重建这件事吗）。
- 典型照法：时间分布数据和你嘴上说的优先级一致吗？哪件事做到一半忘了当初为什么开始？哪些是纯惯性动作？删掉哪件事一个月后不会有任何人（包括你）发现？
- 失效条件：人不是纯理性系统。关系、社群、玩、审美的投入用 ROI 计算会被系统性低估，这面镜子对这类条目只报数据不下结论。

## 镜鉴结构（输出格式）

1. **数据速览**：时间分布表 + 关键计数（项目数/会话数/产出数/烂尾数）；从第二期起所有关键数字带环比
2. **环比裁决（从第二期起必须，放在四镜之前）**：过去两期暴露的每个核心问题 + 每条决议 + 每条预测，逐条判定「实质改变 / 无变化 / 恶化 / 预测应验与否」，附本期证据。只有三种判定写法，不许写「部分改善」这类和稀泥词，边界情况按更差的判。
3. **四镜成像**：每镜 = 观察 3 条（带证据）+ 尖锐提问 2 个 + 建议 1 条。四镜都必须消化双侧对照与协作病理层：乔镜照「你让 AI 做的事该不该存在」，王镜照「你用 AI 骗自己的方式」，库镜照「协作的精度与返工」，一原镜照「浪费的事实」。
4. **作废清单（对抗复核驳回了什么）**：复核整条驳回或查为假的，逐条公示在纪要显要位置，不许悄悄删掉了事。**读者有权知道这份纪要差点告诉他什么。**这一节也是镜的自知第 3 条（负面偏置）唯一的实物证据来源。
5. **浪费账本与禁令**：AI 侧无意义劳动清单 + 每条对应的禁令 / 流程改进。**排序原理：成本函数不是字符（AI 写的字符对照镜人几乎免费），是照镜人的注意力。**每执行一次，省的是 AI 的字符、还是他的注意力、还是下个月不用重讲？只买复利的那条排第一。
6. **工具存量与坑账本**：skill / MCP / 插件三类分开的使用率与加载成本表（含「自建但从未自用」清单、零调用 MCP 的常驻 schema 成本、hook 注入成本）+ 坑的萃取四象限。这一节回答两个问题：**你造的工具有多少在活着？你踩的坑有多少变成了资产？**这两个比率一起下降，说明这个月是在原地打转；一起上升，说明系统在复利。
7. **判断力指纹**：放行后回访率（核心）+ 三条趋势信号 + 反向证据。**这一节是唯一照照镜人自己的一节，写法必须格外克制**：只报可测的行为，不下「你在退化」这类心理判断；数据不支持退化就明确写不支持。
8. **共识与分歧**：四镜同照见的事；镜像冲突的事（原样保留两方论据）
9. **对照镜人问题的直答**：逐条回答照镜人这次带来的问题
10. **决议与跟踪**：最多 3 条决议（多了等于没有）；下次照镜要检查的跟踪项
11. **谥**：四镜合议，为本月上一字谥号，用真实《谥法》字典（去礼远众曰炀、乱而不损曰灵、小心畏忌曰僖、辟地有德曰襄……），附谥法出处与理由。次月凭决议执行情况可改谥。

## 镜的自知（局限声明与改进机制，每次照镜必须附带）

镜承认自己的八条局限：①裁判即同案犯（单模型自审，产出一半是审判者施工的）②只照得见 AI 协作面（线下人生不可见，结论置信度有上限）③负面偏置（被「不够深」驱动的镜有把审视变批判的激励）④单月样本无基线（词频与模式缺意义刻度）⑤动机归因易越界（行为证据撑不起心理断言）⑥药方易违反诊断（决议若无外部钩子，重蹈无 deadline 全烂尾）⑦四镜共享预消化数据包，独立是弱独立 ⑧结论缺可证伪性。

对应机制（逐月执行）：
1. **对抗复核**：每次全镜后加一个独立复核 agent，唯一任务是攻击四镜的证据引用。检查六项：①引文与数字是否属实（**必须回原始文件亲验，不采信取证层的转述**，至少亲验 8 处并公示记录）②解读是否唯一（有没有更平凡的解释）③绝对量词（全部 / 唯一 / 从未）是否被证据覆盖 ④动机断言有没有漏标「推测」⑤与上期结论的重复注水 ⑥**指标的构造效度**（这个数字是不是同义反复）。复核结果与作废清单一并进纪要。经验值：一次真正咬人的复核，整条驳回 3 至 4 条、查假数字 5 至 10 处是正常的；**若某次复核零驳回，先怀疑复核在放水，而不是四镜变准了。**
2. **基线库**：每月语言指纹与关键计数落盘基线库（按月份分节，含会话计数、逐日分布、高频词表、问题清单、决议、预测），下一期照镜第 0 步必读；有两期以上数据后所有词频必须报环比，禁止裸报绝对值。
3. **动机降级**：凡从行为反推心理的句子，必须标注「推测」；不标注的动机断言一律删。
4. **决议带钩子**：每条决议必须写明外部钩子（日历提醒 / cron / 对赌的公开承诺），无钩子的决议不准进纪要。钩子要当场落地：环境里有 cron / 定时任务 / 日历工具就直接建，没有就把钩子写成对照镜人的明确请求（「现在去设一个 X 月 X 日的提醒」）并在下期第 0 步核验钩子是否真建了。
5. **可证伪预测**：每个大结论附一条对下月具体行为的预测，下次照镜首先验预测，预测错了结论降级。

## 镜鉴的视觉规范（HTML 产出）

镜鉴是要被反复打开看的东西，排版本身就是它能不能被读完的一部分。

- **浅色毛玻璃，不是黑底霓虹**：浅色底 + 半透明白玻璃卡片（backdrop-filter 模糊 + 细白边 + 柔影）。科技感来自光与层次。
- **背景是流动的镜中水**：几个低饱和大色斑以极缓的 CSS 动画（40 至 60 秒周期）漂移于底层，玻璃卡浮在其上。镜面本是水，背景应该是活的。记得配 `prefers-reduced-motion` 关掉动画。
- **禁 AI slop 元素**：禁「左边框变色条」式卡片装饰，禁无功能的图标堆砌，禁彩虹渐变标题。强调靠字重、字号、留白、玻璃色温。
- **每份镜鉴至少一幅概念图形**：把当月核心病理画成隐喻图（注意力照不到的地方画成光锥之外、单向阀画成阀门剖面），SVG 手写，数据图表之外必须有这一层。图形先于文字传达判断。
- **交付前机器自检**：div 开闭平衡、零破折号、零斜体、宽视口（1600px+）与窄视口双验，不过不交。

## 运行方式

- **单镜快照**（日常）：一个 agent 依次过四面镜子，产出精简版。
- **全镜**（月度/季度/重大决策）：数据收集（并行）→ 四镜独立成像（并行、互相隔离）→ 对抗复核 → 综合成镜鉴。有 Workflow 类编排工具就用，没有就用并行 subagent。
- **抗中断纪律**：取证 agent 先跑脚本拿到核心数字就立刻落盘一版，之后每完成一节更新一次，不要攒到最后一次性写。长任务被打断是常态，攒着写等于全损。
- 镜鉴归档为 `<归档目录>/jing-YYYY-MM.html`，重要决议写回 memory。
